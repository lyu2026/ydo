name: 打扩展包

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: C
      uses: actions/checkout@v4

    - name: P
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: 依赖
      run: pip install cryptography

    - name: 私钥
      id: keygen
      run: |
        if [ ! -f key.pem ]; then
          openssl genpkey -algorithm RSA -out key.pem -pkeyopt rsa_keygen_bits:2048
        fi

    - name: 打包
      run: |
        # 1. 准备 crx 用的平铺包
        zip -r 译览刀_flat.zip ./* -x "*.git*" ".github/*" "*.md" "*.py" "*.crx" "*.zip" "*.pem"
        python crx.py 译览刀_flat.zip key.pem 译览刀.crx
        rm 译览刀_flat.zip

        # 2. 准备 zip（仅一个 extension 目录）
        mkdir -p build/extension
        shopt -s extglob
        cp -r !(build|.git|.github|*.md|*.py|*.crx|*.zip|*.pem) build/extension/ 2>/dev/null || true
        cd build && zip -r ../译览刀.zip extension/ && cd ..
        rm -rf build

    - name: 上传
      uses: actions/upload-artifact@v4
      with:
        name: chrome-extension
        path: |
          译览刀.crx
          译览刀.zip

    - name: 发布
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v2
      with:
        files: |
          译览刀.crx
          译览刀.zip
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}